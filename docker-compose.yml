services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: biwenger
      POSTGRES_USER: biwenger
      POSTGRES_PASSWORD: biwenger
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: